

describe("order Usecase", () => {
    it("should let users make a order", () => {

    });
    
    it("should let users cancel orders", () => {

    });

    it("should let users add order items", () => {

    });

    it("should let users delete order items", () => {

    });
});